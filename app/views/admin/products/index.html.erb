

<h2>Product List</h2>

<div class="add1 pull-right" >
  <%= link_to("New", new_admin_product_path, class: "fa fa-plus") %>
</div>
<table class="table table-bordered1">
  <thead>
    <tr>
      <th>No.</th>
      <th width="220">Product Pic</th>
      <th>Name</th>
      <th>Price</th>
      <th>Category</th>
      <th width="100"> Options </th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.id %></td>
        <td>
          <%= link_to product_path(product) do %>


            <% if product.photos.present? %>
              <%= image_tag(product.photos[0].avatar.thumb.url, class: "thumbnail3") %>

            <% else %>
              <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail3") %>
            <% end %>
          <% end %>
        </td>
        <td><%= product.title %></td>
        <td><%= product.price %></td>
        <td><%= product.category %></td>
        <td><%= link_to("Edit", edit_admin_product_path(product)) %>
          <%= link_to("Delete", admin_product_path(product)) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
